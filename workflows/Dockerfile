# Use an official lightweight base image
FROM alpine:3.18

# Add a label
LABEL maintainer="smilylife996cha@gmail.com"

# Simple command, replace with your app build steps
CMD ["echo", "Hello from Kaniko build!"]

